{
  "name": "@isbatak/website",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "pnpm --filter @isbatak/panda-ds build && ikona && panda && node scripts/panda-internal-css.mjs && velite build --strict",
    "icons:import": "node scripts/import-tabler-icons.mjs && ikona",
    "dev": "pnpm generate && pnpm run \"/^dev:/\"",
    "dev:panda": "panda dev",
    "dev:velite": "velite dev",
    "dev:next": "next dev --port 3000",
    "build": "pnpm generate && next build",
    "start": "next start",
    "typecheck": "pnpm generate && tsc --noEmit"
  },
  "dependencies": {
    "@ark-ui/react": "5.39.2",
    "@isbatak/panda-ds": "workspace:*",
    "@isbatak/zag-wheel-picker": "workspace:*",
    "@zag-js/react": "catalog:",
    "next": "16.3.6",
    "next-themes": "0.4.6",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@ikona/cli": "0.2.1",
    "@pandacss/compiler": "catalog:",
    "@pandacss/dev": "catalog:",
    "@pandacss/preset-base": "catalog:",
    "@pandacss/transformer": "catalog:",
    "@pandacss/types": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "velite": "0.4.0"
  }
}
